import React from "react";

const PokemonCard = () => {
	return (
		<div>PokemonCard</div>
		// this will be the component that will be called on PokemonCards. It is just the UI
	);
};

export default PokemonCard;
